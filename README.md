# Vault Deployer Plugin

The idea of this plugin is to add vault settings to your project when you deploy.

Ideally if you are running AppRole authentication, you need to generate a new secret when you
perform the deploy. 

